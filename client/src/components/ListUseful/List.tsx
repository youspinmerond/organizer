import { useEffect, useState } from 'react';
import './list.sass';

export default function ListUseful() {

  return (
    <form className="form" onSubmit={(e) => e.preventDefault()}>
      
    </form>
  );
}